@{
    ViewBag.Title = "Home Page";
    
    if (Session["e-mail"] != null)
    {
        Response.Write("<p/>" + Session["e-mail"]);
    }
    if (Session["fullName"] != null)
    {
        Response.Write("<p/>" + Session["fullName"]);
    }
}


    @{ Html.RenderAction("Index", "List"); }
